---
import MainLayout from "../layouts/MainLayout.astro";
import Section from "../components/ui/Section.astro";
import SectionHeading from "../components/ui/SectionHeading.astro";
import Breadcrumbs from "../components/ui/Breadcrumbs.astro";
import { campaignConfig } from "../content/campaign";

const { socialLinks } = campaignConfig;
const breadcrumbItems = [
  { name: "Inicio", path: "/" },
  { name: "Contacto", path: "/contacto" }
];
const contactLinks = [
  {
    href: socialLinks.find((item) => item.label === "Instagram")?.href ?? "#",
    label: "Instagram",
    tone: "text-campaign-sky",
    path: "M7 2h10a5 5 0 0 1 5 5v10a5 5 0 0 1-5 5H7a5 5 0 0 1-5-5V7a5 5 0 0 1 5-5Zm0 2.2A2.8 2.8 0 0 0 4.2 7v10A2.8 2.8 0 0 0 7 19.8h10a2.8 2.8 0 0 0 2.8-2.8V7A2.8 2.8 0 0 0 17 4.2H7Zm10.75 1.65a1.1 1.1 0 1 1 0 2.2 1.1 1.1 0 0 1 0-2.2ZM12 7a5 5 0 1 1 0 10 5 5 0 0 1 0-10Zm0 2.2A2.8 2.8 0 1 0 12 14.8 2.8 2.8 0 0 0 12 9.2Z"
  },
  {
    href: socialLinks.find((item) => item.label === "TikTok")?.href ?? "#",
    label: "TikTok",
    tone: "text-white",
    path: "M14 3c.26 2.08 1.42 3.78 3.6 4.07v2.77a6.3 6.3 0 0 1-3.57-1.17v5.14a5.81 5.81 0 1 1-5.02-5.75v2.86a2.94 2.94 0 1 0 2.17 2.84V3H14Z"
  }
];
const publicContactLinks = contactLinks.filter((item) => /^https?:\/\//.test(item.href));
---

<MainLayout
  path="/contacto"
  title="Contacto | Salvemos Quirilluca"
  description="Canales de contacto de Salvemos Quirilluca para consultas, coordinación y participación comunitaria."
  hideFloatingCta={true}
  keywords={["contacto Quirilluca", "Salvemos Quirilluca", "redes sociales", "consultas"]}
  breadcrumbItems={breadcrumbItems}
  pageType="ContactPage"
>
  <Section size="compact">
    <Breadcrumbs items={breadcrumbItems} />
    <SectionHeading
      eyebrow="Contacto"
      title="Conversemos y mantengamos la campaña en movimiento"
      body="Si quieres difundir, coordinar, hacer una consulta o sumarte desde tu territorio, escríbenos por nuestros canales comunitarios."
      as="h1"
    />
  </Section>

  <Section class="pt-0">
    <div class="mx-auto max-w-5xl">
      <div class="surface-panel rounded-[2rem] p-8 md:p-10">
        <div class="flex flex-wrap items-center justify-center gap-6 md:gap-8">
          {publicContactLinks.map((item) => (
            <a
              href={item.href}
              target={item.href.startsWith("mailto:") ? "_self" : "_blank"}
              rel={item.href.startsWith("mailto:") ? undefined : "noreferrer noopener"}
              aria-label={item.label}
              title={item.label}
              class="focus-ring inline-flex items-center justify-center text-center transition hover:-translate-y-0.5"
            >
              <svg
                aria-hidden="true"
                viewBox="0 0 24 24"
                class={`h-10 w-10 fill-current ${item.tone} drop-shadow-[0_0_6px_rgba(106,141,82,0.62)]`}
              >
                <path d={item.path}></path>
              </svg>
            </a>
          ))}
        </div>

        <div class="mt-8 text-center">
          <div class="flex justify-center text-campaign-sky">
            <svg aria-hidden="true" viewBox="0 0 24 24" class="h-10 w-10 fill-current">
              <path d="M3 5h18a2 2 0 0 1 2 2v10a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V7a2 2 0 0 1 2-2Zm0 2v.51l9 5.63 9-5.63V7H3Zm18 10V9.86l-8.47 5.29a1 1 0 0 1-1.06 0L3 9.86V17h18Z"></path>
            </svg>
          </div>
          <a
            href={`mailto:${campaignConfig.site.contactEmail}`}
            class="focus-ring mt-3 inline-block text-base font-semibold text-white transition hover:text-campaign-warm md:text-lg"
          >
            {campaignConfig.site.contactEmail}
          </a>
        </div>

        <div class="mt-10 grid gap-3 border-t border-white/10 pt-6 text-center sm:grid-cols-3">
          <a href="/transparencia" class="focus-ring rounded-2xl border border-white/12 bg-white/5 px-4 py-3 text-sm text-campaign-text-muted transition hover:text-white">
            Transparencia pública
          </a>
          <a href="/privacidad" class="focus-ring rounded-2xl border border-white/12 bg-white/5 px-4 py-3 text-sm text-campaign-text-muted transition hover:text-white">
            Política de privacidad
          </a>
          <a href="/.well-known/security.txt" class="focus-ring rounded-2xl border border-white/12 bg-white/5 px-4 py-3 text-sm text-campaign-text-muted transition hover:text-white">
            security.txt
          </a>
        </div>
      </div>
    </div>
  </Section>
</MainLayout>

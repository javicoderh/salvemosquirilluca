---
import { campaignConfig } from "../../content/campaign";

const { navigation, site } = campaignConfig;
---

<header class="sticky top-0 z-40 border-b border-campaign-stone/15 bg-campaign-black/88 backdrop-blur-xl">
  <div class="mx-auto flex max-w-7xl items-center justify-between gap-4 px-5 py-4 sm:px-6 lg:px-8">
    <a href="/" class="focus-ring inline-flex items-center gap-4 rounded-full">
      <span class="logo-glow-shell logo-glow-shell--header h-16 w-16 shrink-0">
        <img
          src="/assets/quirilluca/logo-salvemos-quirilluca.svg"
          alt={`Logo de ${site.name}`}
          class="logo-glow-image h-full w-full object-contain brightness-0 invert"
        />
      </span>
      <div>
        <p class="text-xs font-semibold uppercase tracking-[0.16em] text-campaign-warm md:text-sm">Campaña ciudadana</p>
        <p class="text-base font-black text-white md:text-lg">{site.name}</p>
      </div>
    </a>

    <nav aria-label="Principal" class="hidden md:block">
      <ul class="flex items-center gap-6">
        {navigation.map((item) => (
          <li>
            <a
              href={item.href}
              data-analytics-event="navigation_click"
              data-analytics-label={item.label}
              data-analytics-context="site_header_desktop"
              class="focus-ring text-sm font-semibold text-white/88 transition hover:text-campaign-warm"
            >
              {item.label}
            </a>
          </li>
        ))}
      </ul>
    </nav>

    <div class="hidden items-center gap-2 md:flex">
      <!-- Donar: oculto hasta activar Flow
      <a
        href="/#donar"
        data-analytics-event="cta_click"
        data-analytics-label="Donar"
        data-analytics-context="site_header_desktop"
        class="focus-ring button-text-outline inline-flex rounded-full border border-black/40 bg-campaign-warm px-5 py-3 text-sm font-black uppercase tracking-[0.08em] text-black shadow-[0_12px_36px_rgba(208,164,87,0.25),inset_0_1px_0_rgba(255,255,255,0.35)] transition hover:brightness-110"
      >
        Donar
      </a>
      -->
      <a
        href="/firma"
        data-analytics-event="cta_click"
        data-analytics-label="Súmate"
        data-analytics-context="site_header_desktop"
        class="focus-ring inline-flex min-h-14 rounded-full border border-black/60 bg-campaign-sky px-7 py-3.5 text-[0.95rem] font-black uppercase tracking-[0.08em] text-campaign-black shadow-[0_16px_42px_rgba(106,141,82,0.24),0_0_0_1px_rgba(215,192,155,0.14),inset_0_1px_0_rgba(255,255,255,0.22)] transition hover:-translate-y-0.5 hover:border-black/70 hover:brightness-105 hover:shadow-[0_20px_52px_rgba(106,141,82,0.32),0_0_0_1px_rgba(215,192,155,0.2),inset_0_1px_0_rgba(255,255,255,0.26)]"
      >
        Súmate
      </a>
    </div>

    <details class="group relative md:hidden">
      <summary class="focus-ring flex h-12 w-12 cursor-pointer items-center justify-center rounded-full border border-white/14 bg-white/6 text-white marker:hidden list-none transition hover:border-campaign-warm hover:text-campaign-warm">
        <svg aria-hidden="true" viewBox="0 0 24 24" class="h-5 w-5 fill-current group-open:hidden">
          <path d="M4 7h16v2H4zm0 8h16v2H4zm0-4h16v2H4z"></path>
        </svg>
        <svg aria-hidden="true" viewBox="0 0 24 24" class="hidden h-5 w-5 fill-current group-open:block">
          <path d="m6.4 5 5.6 5.6L17.6 5 19 6.4 13.4 12l5.6 5.6-1.4 1.4-5.6-5.6-5.6 5.6L5 17.6 10.6 12 5 6.4 6.4 5z"></path>
        </svg>
      </summary>

      <div class="absolute right-0 top-[calc(100%+0.75rem)] z-50 w-[min(88vw,22rem)] rounded-[1.75rem] border border-campaign-stone/15 bg-campaign-black/95 p-5 shadow-[0_24px_80px_rgba(0,0,0,0.45)] backdrop-blur-xl">
        <nav aria-label="Principal móvil">
          <ul class="grid gap-2">
            {navigation.map((item) => (
              <li>
                <a
                  href={item.href}
                  data-analytics-event="navigation_click"
                  data-analytics-label={item.label}
                  data-analytics-context="site_header_mobile"
                  class="focus-ring block rounded-2xl border border-transparent px-4 py-3 text-sm font-semibold text-white transition hover:border-white/10 hover:bg-white/6 hover:text-campaign-warm"
                >
                  {item.label}
                </a>
              </li>
            ))}
          </ul>
        </nav>

        <div class="mt-4 grid gap-2">
          <!-- Donar: oculto hasta activar Flow
          <a
            href="/#donar"
            data-analytics-event="cta_click"
            data-analytics-label="Donar"
            data-analytics-context="site_header_mobile"
            class="focus-ring button-text-outline flex min-h-12 items-center justify-center rounded-full border border-black/40 bg-campaign-warm px-5 py-3 text-sm font-black uppercase tracking-[0.08em] text-black shadow-[0_12px_36px_rgba(208,164,87,0.25),inset_0_1px_0_rgba(255,255,255,0.35)]"
          >
            Donar
          </a>
          -->
          <a
            href="/firma"
            data-analytics-event="cta_click"
            data-analytics-label="Quiero sumarme"
            data-analytics-context="site_header_mobile"
            class="focus-ring flex min-h-14 items-center justify-center rounded-full border border-black/60 bg-campaign-sky px-6 py-3.5 text-[0.95rem] font-black uppercase tracking-[0.08em] text-campaign-black shadow-[0_16px_42px_rgba(106,141,82,0.24),0_0_0_1px_rgba(215,192,155,0.14),inset_0_1px_0_rgba(255,255,255,0.22)] transition hover:brightness-105"
          >
            Quiero sumarme
          </a>
        </div>
      </div>
    </details>
  </div>
</header>
